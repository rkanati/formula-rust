

fn main() {
    /*let wipeout = std::env::args().nth(1).unwrap();
    let mut bundle = std::io::BufWriter::with_capacity(
        0x10_0000,
        std::fs::File::create("bundle-profile-out").unwrap()
    );
    let mut bytes = Vec::with_capacity(0x10_000);
    bundle::bundler::make_bundle(wipeout, &mut bytes).unwrap();
    use std::io::Write as _;
    bundle.write_all(&bytes[..]).unwrap();*/
}

